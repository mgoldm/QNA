= form_with url: comments_path( commentable_type: commentable.class.name, commentable_id: commentable.id), class: 'new-comment', data: { type: :json } do |f|
  = f.label :comment
  = f.text_field :comment
  = f.submit
